package wd4j.impl.markerInterfaces;

public interface CommandData {
    // A Command DTO marker interface, encapsulating the data of a command before it is sent to the browser via
    // the WebDriverBiDi protocol (WebSocket-Connection).
}
